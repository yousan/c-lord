Create a new discord.py Cog for this project. Follow the add-cog skill for the full checklist.

The Cog name should be: $ARGUMENTS

Steps:
1. Read the `.claude/skills/add-cog/SKILL.md` skill for the complete pattern
2. Create the Cog file at `claude_discord/cogs/{name}.py`
3. Export from `claude_discord/cogs/__init__.py` and `claude_discord/__init__.py`
4. Create tests at `tests/test_{name}.py`
5. Run the verify command to ensure everything passes
