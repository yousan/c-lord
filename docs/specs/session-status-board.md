# あるべき動き: 📊 Session Status ボード

> これは「あるべき動き」。理念 [`docs/PHILOSOPHY.md`](../PHILOSOPHY.md) の下位。迷ったら理念に照らす。
>
> 対象: `DISCORD_CHANNEL_ID` のチャンネルに出る `📊 Session Status` の embed。
> 関連: [thread-lamp.md](./thread-lamp.md)（スレッド名/リアクションのランプ）/ [turn-end-notification.md](./turn-end-notification.md)（ターン終了の呼び出し）。

## これは何か

**「いまどのセッションが動いているか」をチャンネルで一目で見るための板**。スレッドごとに 🟢 Auto-processing / 🟡 Waiting for input と経過時間が並び、状態が変わるたびに**同じメッセージが書き換わる**。

board は**チャンネルに1枚だけ**。ピン留めされていて、📌 を開けば必ずそれが出る。

## あるべき動き（利用者から見て）

### ふだん

- チャンネルに `📊 Session Status` が **1枚**ある。動いているセッションが無いときは `No active sessions.`
- スレッドの状態が変わると、**その1枚が書き換わる**（新しいメッセージは増えない）
- 4時間動いていないスレッドの**行**は板から消える（板そのものは消えない）。**人の投稿や状態の変化が無くても消える** — 板が5分ごとに自分で見回る（#754）。消す行が無い見回りでは Discord に何も送らない
- 最後の行が消えたら `No active sessions.` になる

### 板に載るターン（#754）

**c-lord が Claude を動かすターンは、どの経路でも板に載る。** 動いている間は 🟢 Auto-processing、終わると 🟡 Waiting for input。

| 経路 | 板 | ターン終了の呼び出し（`🟡 Claude has finished …`） |
|---|---|---|
| スレッド／チャンネルへの人の投稿（`ClaudeChatCog`） | 🟢 → 🟡 | 投稿した人を呼ぶ（#481） |
| スケジュール実行（`SchedulerCog`） | 🟢 → 🟡 | 呼ばない（板が変わるだけ） |
| webhook トリガ（`WebhookTriggerCog`、CI/CD 起点） | 🟢 → 🟡 | 呼ばない（板が変わるだけ） |
| `/skill`・`!skill`（`SkillCommandCog`） | 🟢 → 🟡 | 呼ばない（板が変わるだけ） |

- 人が投稿していない経路も載るのは、この板が「**いまどのセッションが動いているか**」の板だから。#754 以前はスケジュール実行が板に一切出ず、動いているセッションが居るのに板には誰も居なかった
- 呼び出しの有無は #754 で**変えていない**。人が投稿していない経路で呼ぶかどうかは #525（`CLORD_OWNER_FALLBACK`）の範囲で、板とは別の関心事
- ターンが例外で終わっても 🟡 に戻る（🟢 のまま残らない）。板の更新に失敗してもターンは止まらない（#632）
- 行の説明文は、人の投稿なら投稿の冒頭、それ以外は**もともと公開されている名前**（`[Scheduled] <タスク名>` / webhook のトリガ名 / `/skill` の行）。スケジュールや webhook の**プロンプト本文は出さない** — サーバ側の設定で、これまで Discord に出ていなかったものだから

### bot を再起動したとき

- **板は増えない。** 起動した bot は、チャンネルに既にある自分の板を**引き継いで書き換える**（#720）
- 前のプロセスが残した**死んだ板は起動時に削除される**。3回再起動しても、チャンネルにある板は1枚のまま
- 📌 を開くと**いま生きている板**が出る。ピンできなかったときは**ログに WARNING が1行残る**（ピンは Discord 側で1チャンネル50件が上限で、埋まると失敗する）

### 触らないもの

- 引き継ぎ・削除の対象は **3条件すべてを満たすメッセージだけ**: **この bot 自身が書いた** / embed のタイトルが `📊 Session Status` / いま生きている板ではない
- 他の bot（staging 等）の板、人の発言、別の embed は**一切触らない**

## 境界

- 板を探す範囲は**チャンネル末尾500件**と**ピン一覧**。ピン一覧も見るのは、📌 を占有している板が履歴の遥か奥にいることがあるため（#720 の本番では 2026-02-24 の板が4枚ピンされていた）
- 1回の起動で消す板は**最大400枚**。それを超える分は次の起動で消える（数千リクエストを一度に投げない）
- 削除に5回失敗したらその回の掃除はやめる（権限が無い、の意味なので粘らない）
- 掃除は**起動をブロックしない**（別タスクで走る）。板の引き継ぎ自体は起動時に同期で終わる
- 履歴もピンも読めない（権限が無い）ときは、**新しい板を1枚出して続行**する。起動は落とさない
- `CLORD_DASHBOARD_SWEEP=0` で**削除だけ**を止められる（引き継ぎは止まらない＝板が増えることはない）

## バグの例（こうなっていたらバグ）

- bot を再起動したら `📊 Session Status` が2枚になった（#720 以前の状態 — 本番で369枚、チャンネルの全メッセージの77%）
- 📌 を開くと、いま動いていない古い板が出る
- 板が1枚も無いのに、スレッドの状態が変わっても何も出ない
- スケジュール実行・webhook・`/skill` のターンが動いているのに板に出ない（#754 以前の状態）
- 人が投稿しない日が続くと、板が何時間も前の行を `0s ago` のまま抱えて凍る（#754 以前の状態 — 本番で47時間）
- 他の bot が書いた板や、人の発言が消えた
- ピンに失敗しているのにログに何も出ない（DEBUG 止まり — #678 の規律違反）

## 根拠

- #67 — 板そのものの導入
- #481 / #495 / #583 — 🟡 に変わったときの呼び出し（誰を・どう呼ぶか）
- #632 / [turn-resilience.md](./turn-resilience.md) — 板の失敗はターンを巻き込まない（板は飾り）
- #678 — 「黙って捨てた」を DEBUG に落として済ませない。ピン失敗が DEBUG 止まりだったせいで、📌 が半年前の板を指していることに誰も気づけなかった
- #720 — 起動のたびに板が増えていた。引き継ぎ＋起動時の掃除はここで入った
- #754 — 板が2日間凍っていた。板に触るのが人の投稿の経路だけで、古い行の刈り取りも状態変化の中でしか走らなかった。全経路のターンを載せ、刈り取りを定期実行にした

## 実装

| 関心事 | 場所 |
|---|---|
| 板の本体（embed・状態） | `c_lord/discord_ui/thread_dashboard.py::ThreadStatusDashboard` |
| 既存の板の引き継ぎ (#720) | `thread_dashboard.py::initialize` / `_find_own_boards` / `_adopt` |
| ピンの確保と失敗の WARNING | `thread_dashboard.py::_ensure_pinned` |
| 死んだ板の掃除 (#720) | `thread_dashboard.py::_sweep_dead_boards`（`CLORD_DASHBOARD_SWEEP=0` で無効） |
| 起動時の呼び出し | `c_lord/bot.py::on_ready`（再接続では作り直さない） |
| 古い行の定期刈り取り (#754) | `thread_dashboard.py::prune_stale_rows` / `_prune_loop`（`initialize` が1本だけ起動） |
| 人が投稿していないターンを載せる (#754) | `thread_dashboard.py::board_turn`（`scheduler.py` / `webhook_trigger.py` / `skill_command.py` から） |
